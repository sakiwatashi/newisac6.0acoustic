# risks.md — 風險與未解清單(2026-07-08,誠實列)

> **⚠️ 2026-07-08 第二輪更新(蓋過下方部分條目)**:E3 系列裁定後,風險版圖大改——
> - **最大風險已升級**:不只 v4,**v9 的 84% vs 29% 也不是聲學閉環證據**(scaffold 模式實錄,見 E3 報告)。README、REPRODUCIBILITY_AUDIT、論文統整稿、docx 中所有「閉環聲學接近優於開環」「不將目標座標作為控制輸入」的敘述都需要重寫。下方第 1–2 條的「回錨 v9」建議**作廢**(D10)。
> - 下方第 4 條(接近段追蹤目標的候選方向)**作廢**:目標在 arm+table 場景聲學不可見已被配對數據證實,特徵工程無物理基礎;僅存的路是改變場景/感測器幾何(N2 診斷)。
> - 新增未驗證項:Physical AI F1≈0.598 的「自體姿態編碼」重詮釋(N1,離線可驗);扳手不可見的具體機制(指向 vs 遮蔽 vs 參數化模型,N2)。
> - Phase A(ρ≈−0.66)與 armfree(r≈0.999)為何目標可見、grasp 場景為何不可見——兩場景的差異(感測器位置/指向、目標尺寸、桌面)尚未逐項歸因。**未驗證**。

## 已裁定但有後果的

1. **v4「100%」敘事已被推翻(F1 CONFIRMED)**,`runtime/SESSION_SUMMARY_2026-07-08.md`、`docs/`、README 中引用 v4 100% 的段落尚未改寫——下一個接手者若只讀 session summary 會被誤導。建議在該檔頭部加棄用聲明。
2. **論文若已把 v4/接近 100% 寫進草稿**(THESIS_DRAFT docx 是否引用未查證——**未驗證**),需要回退到 v9 敘事。
3. E2 的 40% 很好看但不可直接進論文結果表(見 E2 報告的措辭邊界)。放進去而不加幾何條件說明,口試會被一個問題擊穿:「wrench 在 0.9 m 以內時成功率多少?」(答案:0%)。

## 未解的技術問題

4. **接近段不追蹤目標**(根本問題):energy→距離映射在 arm+table 場景無資訊量(F3、規則 2-4)。目前沒有已驗證的替代方案。候選方向(皆未驗證):
   - windowed peak / matched filter 特徵(armfree 已證可行 r≈0.999,但 arm 場景未證);
   - 差分能量(接近時對背景 baseline 做差);
   - 縮短感測器到目標的幾何遮蔽(sensor mount 位置)。
   任何一個都需要先在 arm+table 場景做特徵-距離掃描驗證,再接控制器。
5. **E2 帶內 5 個散發失敗**(wx 1.002–1.229)原因未診斷——可能是 descend 時 Y 向偏差或接觸姿態,需看該幾個 trial 的 grasp_history。**未驗證**。
6. **tier_b 校正表的一切問題原封不動**(單 trial n=19、方向與 Phase A 相反、「offline only」聲明 vs 線上使用)。F1 裁定後它的實際作用已趨近於零(觸發從不生效),但它仍在管線裡,任何人調參後可能讓它重新「起作用」而不自知。
7. **FINAL_APPROACH_STANDOFF_M=0.14 死碼未修**(D2 刻意延後)——若未來重校距離,記得回來處理。
8. **健檢 F5–F12 的中低嚴重度項目多數未處理**(文件數字漂移、重複校正腳本、硬編碼路徑等),見 `docs/HEALTH_CHECK_2026-07-08.md`。

## 本輪引入的技術債(可控)

9. `GRASP_BLIND_APPROACH` spike 旗標留在 `ultrasonic_grasp_common.py`(預設關閉、有標記註解、plan.md 有回滾指引)。裁定已完成,論文定稿前可移除,或保留作為指標失效偵測工具——**決策未做,留給用戶**。
10. 工作樹更髒了:本輪新增 ~10 個檔案 + 動了 2 個既有檔(common 的旗標、test 修復)。**全部未 commit**(D5)。用戶需自行決定 commit 切分。

## 統計上的誠實聲明

11. E1 盲走「略優」(30/30 vs 29/30、20/30 vs 16/30)是雜訊級差異(n=30、單次 run、無重複),只支持「無差異」結論,不支持「聲學有害」。
12. E2 的 71% 帶內條件成功率樣本只有 17,置信區間很寬(~±22%)。要引用需重跑更多 seeds。**未驗證重複性**。
13. 所有實驗都是單一 Isaac Sim 版本(6.0.0-rc.59)、單機、單 seed 系列——與 repo 一貫的 claim boundary 一致,但交接者别把任何數字外推到其他配置。
